name: Secret Environment Variable Check

on:
  workflow_dispatch:

jobs:
  check-secret:
    runs-on: ubuntu-latest

    env:
      MOCK_API_KEY: ${{ secrets.MOCK_API_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check secret
        run: python check_secret.py
